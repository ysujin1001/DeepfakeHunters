# 🧠 Prototype ResNet (Yun Sujin Ver.)

이 폴더는 딥페이크 / 조작 이미지 탐지 실험용 프로토타입 모델입니다.  
외국인 얼굴 데이터셋(예: Kaggle 공개셋)을 사용해 제작되었으며,  
ResNet-18을 기반으로 한 간단한 분류 모델을 테스트하기 위한 구조입니다.

---

## 📦 환경 설정
```bash
# 필수 패키지 설치
pip install torch torchvision pillow tqdm opencv-python pandas
```

## 📁 폴더 구조
```bash
prototype_resnet_yunsujin/
│
├── make_edited_dataset.py      # Real 이미지를 변형해 Edited 세트 생성
├── deepfake_detect.py          # ResNet18 학습 및 단일 테스트
├── deepfake_test_edited.py     # Edited 폴더에 대해 모델 판별 테스트
│
└── output/
    └── model_resnet_best.pth   # 학습된 모델 가중치 (자동 생성)
```

## 🧩 코드 설명
1️⃣ make_edited_dataset.py
Real 이미지를 인위적으로 변형하여 Edited 데이터셋을 만드는 코드입니다.
딥페이크 합성 수준은 아니며, 단순한 조작(노이즈, 블러, 밝기/대비 변경 등)만 수행합니다.

실행 방법 (경로는 ai/prototype_resnet_yunsujin 로 변경해야 함)
```bash
python make_edited_dataset.py
```

2️⃣ deepfake_detect.py
Real/Fake 이미지를 분류하기 위한 ResNet-18 기반 학습 코드입니다.
각 클래스(Real, Fake)에서 무작위로 500장씩 샘플링해 학습하며,
학습이 완료되면 모델 가중치가 output/model_resnet_best.pth로 저장됩니다.

실행 방법 (경로는 ai/prototype_resnet_yunsujin 로 변경해야 함)
```bash
python deepfake_detect.py
```

3️⃣ deepfake_test_edited.py
앞서 생성한 Edited 폴더의 이미지들을 대상으로,
deepfake_detect.py에서 학습한 모델(model_resnet_best.pth)을 사용해
각 이미지가 Real인지 Fake인지 예측하는 코드입니다.

모델의 일반화 성능을 확인하기 위한 테스트 스크립트로,
실행 결과는 터미널에서 통계 형태로 출력됩니다.

실행 방법 (경로는 ai/prototype_resnet_yunsujin 로 변경해야 함)
```bash
python deepfake_test_edited.py
```